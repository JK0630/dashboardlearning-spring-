<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="kr.ac.kopo.dashboard.mapper.CommonMapper">
    <select id="selectInoutStatus" resultType="map">
        SELECT seq, name, dlc_in, dlc_out, pmc_in, pmc_out
        FROM inout_status
    </select>
</mapper>